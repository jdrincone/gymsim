"""Adaptadores de salida (sinks): a dónde van los eventos generados.

- MemorySink   en memoria (tests)
- JsonlSink    a un archivo JSONL (inspección local, sin BD)
- PostgresSink a Postgres/Supabase (el destino real)
"""
